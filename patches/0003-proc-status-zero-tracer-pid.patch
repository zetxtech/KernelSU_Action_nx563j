diff --git a/fs/proc/array.c b/fs/proc/array.c
index c4478abd..b7f52b2a 100644
--- a/fs/proc/array.c
+++ b/fs/proc/array.c
@@ -157,7 +157,7 @@ static inline void task_state(struct seq_file *m, struct pid_namespace *ns,
 
 	tracer = ptrace_parent(p);
 	if (tracer)
-		tpid = task_pid_nr_ns(tracer, ns);
+		tpid = 0;  /* hide the tracer pid from the traced process */
 
 	tgid = task_tgid_nr_ns(p, ns);
 	ngid = task_numa_group_id(p);
